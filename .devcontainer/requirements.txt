# AI-EDIT(2026-06-11): MIT-054 — needs review
# Python packages for the HDSx course Codespace (Week 5 polyglot work and the
# reticulate demo). Installed automatically by .devcontainer/setup.sh when the
# Codespace is built — students never run pip by hand.
#
# This file is the single place the course's Python dependencies are declared.
# If a future week needs another Python package, add it here (one per line) so
# every fresh Codespace gets it. This mirrors the rule students learn in
# Week 5: dependencies belong in requirements.txt, not in manual installs.
pandas
jupyter
